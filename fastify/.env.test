# Test environment configuration

# Server
PORT=3001
HOST=localhost
NODE_ENV=test

# JWT
JWT_SECRET=test_jwt_secret_for_testing_purposes_only
JWT_EXPIRATION=1h
JWT_REFRESH_EXPIRATION=7d

# Supabase (Use mock values for testing)
SUPABASE_URL=https://test-url.supabase.co
SUPABASE_SERVICE_ROLE_KEY=test_service_role_key
SUPABASE_ANON_KEY=test_anon_key

# Plaid (Use mock values for testing)
PLAID_CLIENT_ID=test_plaid_client_id
PLAID_SECRET=test_plaid_secret
PLAID_ENV=sandbox 